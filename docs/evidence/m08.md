# M08 acceptance evidence

Date: 2026-08-19

| Gate | Result |
|---|---|
| Format and workspace clippy (`-D warnings`) | passed |
| `make test-integration` | passed |
| Messaging property test | seeded/generated offset permutations never acknowledged across a hole |
| Real Redpanda recoverable-gap test | refund v2 published before authorization v1; both applied in version order, no DLQ |
| Real Redpanda stale test | one applied, one counted stale, one provider authorization |
| Optimistic concurrency | existing SQLx stale-version test confirms zero-row update returns typed `VersionConflict` |
| Replay safety | replay CLI republishes the original serialized envelope and event/correlation identity, adding only replay headers; business consumers remain inbox/provider-idempotent |

The shared `ContiguousOffsetTracker` is the critical crash-safety mechanism:
processing may be reordered, but the durable offset advances only through a
contiguous prefix. Unrecoverable gaps still use `EXPECTED_VERSION_GAP`.

Replay usage:

```bash
cargo run -p replay-dlq -- payments.commands.v1 12 localhost:19092
```

The CLI refuses records without a parseable envelope or whose embedded source
topic does not match the requested topic.
